<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.meomulm.notification.model.mapper.NotificationMapper">

    <!-- 회원 알림 조회 -->
    <select id="selectNotificationByUserId" resultType="Notification">
        SELECT *
        FROM notification
        WHERE user_id = #{userId}
    </select>

    <!-- 회원 알림 추가 -->
    <insert id="insertNotification" parameterType="Notification" useGeneratedKeys="true" keyProperty="notificationId">
        INSERT INTO notification(user_id, notification_content, notification_link_url, is_read, created_at)
        VALUES (#{userId}, #{notificationContent}, #{notificationLinkUrl}, false, NOW())
    </insert>

    <!-- 알림 읽음 상태 변경 -->
    <update id="updateNotificationStatus">
        UPDATE notification
        SET is_read = true
        WHERE notification_id = #{notificationId}
    </update>

    <!-- 알림 삭제 -->
    <delete id="deleteNotification">
        DELETE from notification
        WHERE notification_id = #{notificationId}
    </delete>

</mapper>